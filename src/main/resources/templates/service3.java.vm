package $!{package.Service};

import $!{package.Entity}.$!{entity};
#set($voPackage=$package.Entity.replace("entity","vo"))
import $!{voPackage}.$!{entity}VO;
import com.neusoft.core.restful.AppResponse;
import com.github.pagehelper.PageInfo;

import java.util.List;

/**
 * $!{table.comment} 服务类
 *
 * @author $!{author}
 * @since $!{date}
 */
#if($!{kotlin})
interface $!{table.serviceName} : $!{superServiceClass}<$!{entity}>
#else
public interface $!{table.serviceName}  {

        /**
         * 详情 $!{table.comment}
         */
        AppResponse<$!{entity}VO> get$!{entity}Info(String id);

        /**
         * 分页 $!{table.comment}
         */
        AppResponse<PageInfo<List<$!{entity}VO>>> select$!{entity}ByPage($!{entity} user);

        /**
         * 新增 $!{table.comment}
         */
        AppResponse save$!{entity}($!{entity} user);

        /**
         * 修改 $!{table.comment}
         */
        AppResponse update$!{entity}ById($!{entity} user);

        /**
         * 删除 $!{table.comment}
         */
        AppResponse  delete$!{entity}Logic(String ids);

}
#end
